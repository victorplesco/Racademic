v2_validate_headers <- function(headers) {
  
  if(response[[1]]$status == "200")
  {}
  response[[1]]$status
  object.size(tmp_response$content)

};

# check_response(response = test_response)
# headers
